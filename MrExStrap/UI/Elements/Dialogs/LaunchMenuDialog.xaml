<base:WpfUiWindow x:Class="MrExStrap.UI.Elements.Dialogs.LaunchMenuDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
        xmlns:local="clr-namespace:MrExStrap.UI.Elements.Dialogs"
        xmlns:base="clr-namespace:MrExStrap.UI.Elements.Base"
        xmlns:models="clr-namespace:MrExStrap.UI.ViewModels"
        xmlns:resources="clr-namespace:MrExStrap.Resources"
        mc:Ignorable="d"
        Title="Bloxstrap - Mr Exploit Edition"
        MinWidth="0"
        MinHeight="0"
        Width="580"
        SizeToContent="Height"
        ResizeMode="NoResize"
        Background="{ui:ThemeResource ApplicationBackgroundBrush}"
        ExtendsContentIntoTitleBar="True"
        WindowStartupLocation="CenterScreen">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <ui:TitleBar Grid.Row="0" Grid.ColumnSpan="2" Padding="8" ShowMinimize="False" ShowMaximize="False" Title="Bloxstrap - Mr Exploit Edition" Icon="pack://application:,,,/MrExStrap.ico" CanMaximize="False" KeyboardNavigation.TabNavigation="None" />

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="280" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <Grid Grid.Column="0" VerticalAlignment="Center">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>
                
                <Grid Grid.Row="0" HorizontalAlignment="Center" Margin="0,0,0,32">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>

                    <Image Grid.Column="0" Width="64" Height="64" Source="pack://application:,,,/Resources/MrExploit.png" RenderOptions.BitmapScalingMode="HighQuality" />
                    <StackPanel Grid.Column="1" Margin="12,0,0,0" VerticalAlignment="Center">
                        <TextBlock Text="Bloxstrap&#x0a;Mr Exploit Edition" FontSize="20" LineHeight="22" />
                        <TextBlock Text="{Binding Version, Mode=OneTime}" VerticalAlignment="Bottom" FontSize="12" Foreground="{DynamicResource TextFillColorTertiaryBrush}" />
                        <Border Margin="0,6,0,0" Padding="8,3,8,3" CornerRadius="10" HorizontalAlignment="Left"
                                Background="{Binding ChannelLockBackground, Mode=OneTime}">
                            <TextBlock Text="{Binding ChannelLockText, Mode=OneTime}" FontSize="11" FontWeight="SemiBold" Foreground="White" />
                        </Border>
                    </StackPanel>
                </Grid>

                <StackPanel Grid.Row="1" HorizontalAlignment="Center">
                    <ui:Hyperlink Icon="QuestionCircle48" Content="{x:Static resources:Strings.About_Title}" Command="{Binding LaunchAboutCommand, Mode=OneTime}" HorizontalAlignment="Center" />
                    <ui:Hyperlink Icon="PeopleCommunity28" Content="Roblox Exploit Community" NavigateUri="https://robloxscripts.com/" HorizontalAlignment="Center" />
                    <ui:Hyperlink Icon="PlayCircle28" Content="Mr Exploit's YouTube" NavigateUri="https://www.youtube.com/@RealMrExploit-f5h" HorizontalAlignment="Center" />

                    <!-- RSWARE.STORE CTA banner. Clickable area is the whole border via the
                         MouseBinding below — Cursor=Hand gives the visual cue. Style trigger
                         brightens the background on hover. -->
                    <Border Margin="12,16,12,4" Padding="14,12" CornerRadius="6"
                            BorderBrush="#3B82F6" BorderThickness="1"
                            Cursor="Hand">
                        <Border.Style>
                            <Style TargetType="Border">
                                <Setter Property="Background" Value="#143B82F6" />
                                <Style.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter Property="Background" Value="#2A3B82F6" />
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </Border.Style>
                        <Border.InputBindings>
                            <MouseBinding MouseAction="LeftClick"
                                          Command="models:GlobalViewModel.OpenWebpageCommand"
                                          CommandParameter="https://rsware.store/" />
                        </Border.InputBindings>
                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center"
                                    ToolTip="https://rsware.store/">
                            <ui:SymbolIcon Symbol="ShoppingBag24" FontSize="22"
                                           Foreground="#3B82F6"
                                           Margin="0,0,10,0" VerticalAlignment="Center" />
                            <TextBlock VerticalAlignment="Center" TextAlignment="Center"
                                       Foreground="#3B82F6" TextWrapping="Wrap" MaxWidth="200">
                                <Run FontSize="15" FontWeight="Bold">RSWARE.STORE</Run>
                                <LineBreak />
                                <Run FontSize="11" FontWeight="SemiBold">BUY ROBLOX EXECUTORS &amp; EXTERNALS HERE</Run>
                            </TextBlock>
                        </StackPanel>
                    </Border>
                </StackPanel>
            </Grid>

            <StackPanel Grid.Column="1" Margin="16">
                <ui:CardAction Icon="ArrowRight12" TabIndex="0" Command="{Binding LaunchRobloxCommand, Mode=OneTime}">
                    <StackPanel>
                        <TextBlock FontSize="14" Text="{x:Static resources:Strings.LaunchMenu_LaunchRoblox}" />
                    </StackPanel>
                </ui:CardAction>

                <ui:CardAction Margin="0,8,0,0" TabIndex="1" Icon="Settings28" Command="{Binding LaunchSettingsCommand, Mode=OneTime}">
                    <StackPanel>
                        <TextBlock FontSize="14" Text="{x:Static resources:Strings.LaunchMenu_ConfigureSettings}" />
                    </StackPanel>
                </ui:CardAction>

            </StackPanel>
        </Grid>
    </Grid>
</base:WpfUiWindow>
